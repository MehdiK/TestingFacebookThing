<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title - My ASP.NET MVC Application</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />
        <meta property="og:url" content="http://testingfacebookthing.azurewebsites.net"/>
    </head>
    <body>
        <header>
            @System.DateTime.UtcNow.ToString("u") 
        </header>
        <div id="body">
            @RenderBody()
            <form class="edit-pizza-name-form" name="editNameForm" ng-submit="save()" ng-show="isEditing">
                <p>Test Content inside form</p>
                <button type="submit">Done</button>
            </form>
        </div>
    </body>
</html>
